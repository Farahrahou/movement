<mvc:View
    controllerName="be.ap.sap.movapp.controller.Detail"
    xmlns="sap.m"
    xmlns:semantic="sap.f.semantic"
    xmlns:f="sap.ui.layout.form"
    xmlns:mvc="sap.ui.core.mvc">

    <semantic:SemanticPage
        id="detailPage"
        busy="{detailView>/busy}"
        busyIndicatorDelay="{detailView>/delay}"
        showFooter="true">
        <semantic:titleHeading>
            <Title
                text="{MovId}"
                level="H2"/>
        </semantic:titleHeading>
        <semantic:content>

        </semantic:content>
        <!--Semantic ShareMenu Buttons-->
        <semantic:sendEmailAction>
            <semantic:SendEmailAction
                id="shareEmail"
                press=".onSendEmailPress"/>
        </semantic:sendEmailAction>

        <semantic:closeAction>
            <semantic:CloseAction
                    id="closeColumn"
                    press=".onCloseDetailPress"/>
        </semantic:closeAction>

        <semantic:fullScreenAction>
            <semantic:FullScreenAction
                    id="enterFullScreen"
                    visible="{= !${device>/system/phone} &amp;&amp; !${appView>/actionButtonsInfo/midColumn/fullScreen}}"
                    press=".toggleFullScreen"/>
        </semantic:fullScreenAction>
        <semantic:exitFullScreenAction>
            <semantic:ExitFullScreenAction
                    id="exitFullScreen"
                    visible="{= !${device>/system/phone} &amp;&amp; ${appView>/actionButtonsInfo/midColumn/fullScreen}}"
                    press=".toggleFullScreen"/>
        </semantic:exitFullScreenAction>
           <semantic:content>
            <f:SimpleForm>
                <f:content>
                    <Label text="MovId" visible="{detailView>/new}" />
                    <Input value="{MovId}" visible="{detailView>/new}"/>
                    <Label text="MovId" visible="{!${detailView>/new}"/>
                    <Text text="{MovId}" visible="{!${detailView>/new}" />
                    <Label text="Type" />
                    <Input value="{Type}" />
                    <Label text="MovDate" />
                    <DatePicker value="{ path: 'MovDate', type: 'sap.ui.model.type.Date' }" displayFormat="dd.MM.yyyy" />
                    <Label text="ChgDate" />
                    <DatePicker value="{ path: 'ChgDate', type: 'sap.ui.model.type.Date' }" displayFormat="dd.MM.yyyy" />
                    <Label text="ChgUser" />
                    <Input value="{ChgUser}" />
                    <Label text="Partner" />
                    <Input value="{Partner}" />
                    <Label text="Location" />
                    <Input value="{Location}" />
                    <Label text="Finished" />
                    <CheckBox selected="{Finished}" />
                </f:content>
            </f:SimpleForm>

        </semantic:content>

        <semantic:footerCustomActions>
			<Button text="Save" press="onSave" />
        </semantic:footerCustomActions>

    </semantic:SemanticPage>

</mvc:View>